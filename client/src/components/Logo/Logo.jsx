import logo from "../../assets/images/pokemon-logo.svg";

const Logo = () => {
    return (
        <div className="Logo">
            <img src={ logo } />
        </div>
    );
};

export default Logo;
